# Auto-generated improvement — Iteration 91
# Suggestion: Add a check for the 'content-type' attribute in the `handle_starttag` method to ignore scripts, stylesheets, and other non-breaking tags

def The user wants a short Python function for a Raspberry Pi that adds a check for 'content-type' attribute in handle_starttag method to ignore scripts, stylesheets, and other non-HTML content. Max 20 lines, no docstring longer than 1 line, stdlib only. Stop after closing return statement.

I need to write a function that likely extends HTMLParser or similar. The function should be a method or a class? The prompt says "Write a short Python function (max 20 lines) for a Raspberry Pi. Task: Add a check for the 'content-type' attribute in the `handle_starttag` method to ignore scripts, stylesheets, and other n". It seems they want a function that modifies handle_starttag. Probably they want a class that inherits from html.parser.HTMLParser and overrides handle_starttag. But they said "function". Could be a function that returns a parser class? Or a function that does parsing? The instruction: "Add a check for the 'content-type' attribute in the `handle_starttag` method to ignore scripts, stylesheets, and other n". Likely they want a subclass of HTMLParser with handle_starttag that checks content-type attribute.

But they said "Write a short Python function (max 20 lines)". Could be a function that defines a class and returns it? Or just a class definition? The prompt ends with "def ". So they want a function definition starting with "def ". Probably they want a function that creates a parser? But the task is to add a check in handle_starttag method. That suggests we are defining a class with that method. However, the prompt says "def " at the end, meaning they want the code to start with a function definition. Maybe they want a function that returns a parser instance? Or a function that does the parsing? Let's read carefully: "Write a short Python function (max 20 lines) for a Raspberry Pi.
