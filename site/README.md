# freeWillAi landing page

Static one-page site describing the freeWillAi project. No build step — plain HTML/CSS.

## Deploy to Vercel

```bash
cd ~/site
vercel deploy --prod
```

Or connect this folder as a project in the Vercel dashboard — it will be auto-detected
as a static site (no framework, no build command needed).
